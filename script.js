// Part 1: the interface is ready. Part 2 will request and render real images.
const searchForm = document.querySelector("#search-form");

searchForm.addEventListener("submit", (event) => {
  event.preventDefault();
  // Search behaviour will be added in the next lesson.
});